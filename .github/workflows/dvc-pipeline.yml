name: Train Test, Infer and Report
on:
  workflow_call:
  workflow_dispatch:

jobs:
  train-and-report:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      repository-projects: write
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: iterative/setup-cml@v2
      
      - name: Install uv
        uses: astral-sh/setup-uv@v2

      - name: Set up Python 3.12
        run: uv python install 3.12

      - name: Install dependencies
        env:
          UV_EXTRA_INDEX_URL: https://download.pytorch.org/whl/cpu
        run: |
          uv sync
      - name: Pull data from DVC
        env:
          GDRIVE_CREDENTIALS_DATA: ${{ secrets.GDRIVE_CREDENTIALS_DATA }}
        run: |
          mkdir -p .dvc/cache
          chmod -R 777 .dvc/cache
          ls -la .dvc/cache
          echo $GDRIVE_CREDENTIALS_DATA > gdrive_credentials.json
          uv run dvc remote modify --local myremote gdrive_service_account_json_file_path gdrive_credentials.json
          uv run dvc pull -v

      - name: Train model
        run: |
          uv run dvc repro

      - name: Create CML report
        env:
          REPO_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Generate acc, loss plots and confusion matrix table plots
          rm report.md
          sh report_gen.sh
          cml comment create report.md